apiVersion: v1
kind: Secret
metadata:
  name: api-valkey-auth
  labels:
    secret-generator.cs.sap.com/enabled: "true"
stringData:
  username: "api-valkey"
  password: "%generate:password:length=24"
